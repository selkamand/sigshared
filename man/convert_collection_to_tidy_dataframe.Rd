% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conversions.R
\name{convert_collection_to_tidy_dataframe}
\alias{convert_collection_to_tidy_dataframe}
\title{Convert Signature or Catalogue Collection to a Tidy Data Frame}
\usage{
convert_collection_to_tidy_dataframe(signatures)
}
\arguments{
\item{signatures}{A signature or catalogue collection. A named list of data frames, each containing
mutation information (see \code{\link[=example_signature_collection]{example_signature_collection()}} or \code{\link[=example_catalogue_collection]{example_catalogue_collection()}}).}
}
\value{
A tidy data frame with columns:
\itemize{
\item \code{signature} or \code{catalogue}: the name of the signature
\item \code{type}: the mutation type (e.g., "T>C")
\item \code{channel}: the channel (e.g., "A[T>C\]G")
\item \code{fraction}: the normalized mutation fraction
\item \code{count} (only for catalogue collections): the mutation count
}

[T>C\]: R:T\%3EC\%5C\%5C\%5C
}
\description{
Converts a collection of signatures or catalogues data frames into a single tidy data frame.
}
\details{
The function automatically detects whether the input is a signature collection or a catalogue collection,
and appends a column (named after the collection type) to identify the originating signature/catalogues.
}
\examples{
sigs <- example_signature_collection()
tidy_df <- convert_collection_to_tidy_dataframe(sigs)

cats <- example_catalogue_collection()
tidy_cat <- convert_collection_to_tidy_dataframe(cats)
}
